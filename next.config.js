// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
   images: {
    domains: ['res.cloudinary.com'], // domain cloudinary yang kamu pakai
  },
}

module.exports = nextConfig
